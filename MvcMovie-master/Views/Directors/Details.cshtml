@model MvcMovie.Models.Director

@{
    ViewData["Title"] = "Details";
}

<h5 class="page-title text-light"> Directors | Details</h5>
<div>
    <hr />
    <dl class="row text-light">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
</div>
<table class="table table-light table-striped">
    <thead>
        <tr>
            <th></th>
            <th>
                Title
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Movies)
        {
            <tr>
                <td></td>
                <td>
                    @item.Title
                </td>
                <td></td>
                <td></td>
                <td></td>
                <td>
                    <div class="btn-group btn-group-sm" role="group" aria-label="Basic example">
                        <a class="btn btn-small btn-success" asp-action="Edit" asp-route-id="@Model.ID">Edit</a>
                        <!--  <a class="btn btn-small btn-danger" asp-action="Delete" asp-route-id="">Remove</a> -->
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>
<div>

    <a class="text-light" asp-action="Index" >Back to List</a>
</div>
